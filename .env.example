# Required Environment Variables
# Copy this file to .env and fill in your values

# Webhook secret for HMAC signature verification
WEBHOOK_SECRET=your-secret-key-here

# SSH Configuration (shared across all services)
SSH_USER=deploy
SSH_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----
...your private key here...
-----END PRIVATE KEY-----"

# Service: DigletBot
DIGLETBOT_SSH_HOST=example.com
DIGLETBOT_SSH_PORT=22
DIGLETBOT_SSH_COMMAND="docker compose pull && docker compose up -d digletbot"

# Add more services as needed:
# Service: ServiceB (example)
# SERVICEB_SSH_HOST=serviceb.example.com
# SERVICEB_SSH_PORT=22
# SERVICEB_SSH_COMMAND="/path/to/update-serviceb.sh"

# Server Configuration
PORT=3000
HOST=0.0.0.0

# Logging
LOG_LEVEL=info
NODE_ENV=development
